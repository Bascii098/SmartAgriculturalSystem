import { Typography } from 'antd'

const { Title } = Typography

function GIS() {
  return (
    <div>
      <Title level={2}>GIS可视化模块</Title>
      <p>地图可视化与空间数据分析</p>
    </div>
  )
}

export default GIS
